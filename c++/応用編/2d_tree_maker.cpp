#include "../taichitary.h"

class Node{
public:
    int location;
    int p, l, r;
    Node() {}; //これはなに
};

class Point{
public:
    int id, x, y;
    Point(){}
    Point(int id, int x, int y): id(id), x(x), y(y){}
    bool operator < (const )
};

int main(){

}